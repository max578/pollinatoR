% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spec.R
\name{pollin_spec}
\alias{pollin_spec}
\title{Declare a pollinator-exclusion study}
\usage{
pollin_spec(
  data,
  outcomes,
  treatment,
  control,
  random,
  exposures = NULL,
  adjust = NULL,
  group = NULL,
  latent_unit = NULL
)
}
\arguments{
\item{data}{A data frame, one row per observational unit (e.g. one plant).}

\item{outcomes}{Character vector of outcome column names (yield components).}

\item{treatment}{Name of the treatment column (a factor with two or more
levels, e.g. caged versus open).}

\item{control}{Treatment level that represents exclusion / the reference
(e.g. \code{"Cage"}). The contribution is computed relative to this level.}

\item{random}{Character vector of grouping columns giving the nesting, outer
to inner (e.g. \code{c("Site", "Distance", "Transect")}). Translated into nested
random intercepts.}

\item{exposures}{Optional character vector of exposure columns (e.g. insect
abundances) used by \code{\link[=pollin_moderation]{pollin_moderation()}}.}

\item{adjust}{Optional character vector of adjustment covariates (e.g.
landscape, climate) -- context, not the inferential target.}

\item{group}{Optional name of a fixed grouping column (e.g. region).}

\item{latent_unit}{Optional name of the unit carrying the random treatment
slope (defaults to the outermost \code{random}). This absorbs unobserved
unit-level modifiers of the contribution, such as genotype.}
}
\value{
An object of class \code{pollin_spec}.
}
\description{
Captures the structure of an open-versus-exclusion experiment in one object
so the downstream verbs share a single, validated description of the data.
The specification is deliberately general: outcomes, the exclusion treatment,
the nested random structure, optional exposures (the pollinator community),
optional adjustment covariates, an optional fixed grouping, and an optional
latent unit on which a random treatment slope is carried to absorb
unobserved between-unit heterogeneity (for example an unrecorded genotype).
}
\examples{
df <- data.frame(
  y = rpois(40, 5), trt = rep(c("Cage", "Open"), 20),
  Site = rep(letters[1:4], each = 10)
)
pollin_spec(df, outcomes = "y", treatment = "trt", control = "Cage",
            random = "Site")
}
