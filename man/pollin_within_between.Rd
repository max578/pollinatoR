% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{pollin_within_between}
\alias{pollin_within_between}
\title{Within-between (Mundlak) decomposition of a numeric variable}
\usage{
pollin_within_between(x, group)
}
\arguments{
\item{x}{Numeric vector.}

\item{group}{Grouping factor of the same length as \code{x}.}
}
\value{
A list with elements \code{within} and \code{between}, each the length of \code{x}.
}
\description{
Splits a numeric vector into a between-group part (the group mean, broadcast
back to each row) and a within-group part (the deviation from that mean).
Used to separate the within-unit component of an exposure -- which is robust
to any unit-level confounder, observed or not -- from the between-unit
component.
}
\examples{
wb <- pollin_within_between(c(1, 3, 10, 12), c("a", "a", "b", "b"))
wb$between
wb$within
}
